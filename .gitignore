# Dependencies
node_modules/

# Build output (mcp-server/dist is generated; the committed plugin bundle
# at plugins/bridgistic/server/index.js is intentionally NOT ignored).
mcp-server/dist/

# Distribution packages
dist/

# Environment files — never commit real credentials
.env
.env.*
!.env.example
connections.json

# OS / editor noise
.DS_Store
Thumbs.db
*.log
